; included #include <stdio.jc>

global k_main
k_main:
  push rbp
  mov rbp, rsp
  ; ptr screen = 0xB8000;
  mov qword [rbp-8], 0xB8000
  ; u32 status = 0;
  mov dword [rbp-12], 0
  mov rsp, rbp
  pop rbp
  ret