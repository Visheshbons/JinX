// kernel/main.jc
// JC is JinX's C

#include <stdio.jc>

u32 k_main() {
    // This is where JinX begins
    ptr screen = 0xB8000;
    u32 status = 0;

    println("Kernel loaded")

    return status;
}
